# 首版验收记录

日期：2026-09-20。运行环境：macOS、Node.js 24.18.0、pnpm 10.34.0。

## 自动化

34 项测试覆盖：

- 四席独立评审、整理、讨论、修订及复核的完整流程。
- 24 轮、103 次模拟调用：验证流程不固定在 11 次调用结束。此项不等于真实模型已经运行 4 小时。
- 首轮上下文隔离、统一揭示、编辑包含最后一轮交叉意见。
- 原子预算预留、实际 usage 结算、失败／中断保守计入、最多三次尝试。
- 最多两个运行任务、Profile 最多四个请求并发、实时进度不阻塞暂停。
- 取消／暂停后的迟到响应不写入结论，重启恢复不自动重发。
- 跨 Workspace/Session 读取、操作和导出拦截；设置等修改的 revision 冲突检查。
- Host、socket、Origin、Fetch Metadata、随机访问凭证、请求大小限制。
- Chat Completions、Messages、Responses 的适配，DSH 原生流模拟；身份不一致、HTTP 200 上游错误、截断和缓存用量。
- 原生 DSH Host 注册 Skill、工具、路由和 storageDomain 契约；卸载清理。
- 原子文件更新、损坏存储拒绝启动、存储预留失败不调用模型、HTML 转义。
- 反馈创建新版本、人工取舍保持旧异议、缺少证据的事实不会自动成为已证实。

## 浏览器完整交互

使用实际 React 页面、本地 Host 服务、确定性演示模型和无头 Chromium：

1. 填写方案，配置四个角色和讨论限制，创建并开始。
2. 完成 11 次模拟模型调用，查看四项问题和完整修订稿。
3. 填写人工取舍理由并采纳，下载 HTML 报告。
4. 在 1440px 桌面及 390px 手机宽度检查页面，未发现横向溢出或浏览器脚本错误。
5. 补充事实创建 V2，暂停、刷新页面、恢复并完成；保留两个历史版本。

截图：`test-results/setup-desktop.png`、`discussion-desktop.png`、`report-desktop.png`、`report-mobile.png`。测试制品默认不提交 Git。

## 真实网关完整验收

使用现有环境配置，仅提交“内部公开帮助文档问答试点”的合成方案。没有发送用户业务文件或其他仓库内容。

最终验收任务：`c4ad6bb7-1db9-400a-afaf-b07554e4d72c`。完成于 `2026-09-20T01:45:54.254Z`。运行配置：一轮交叉讨论、最多 16 次调用、15 分钟、60 万 Token 保守预留上限。

| 模型     | 独立评审 | 交叉回应 | 其他阶段               | 返回 model            |
| -------- | -------- | -------- | ---------------------- | --------------------- |
| 千问     | 成功     | 成功     | 整理问题、完整修订成功 | `qwen3.8-max`         |
| Kimi     | 成功     | 成功     | 独立复核成功           | `kimi-k2.6`           |
| 智谱     | 成功     | 成功     | —                      | `glm-5.1`             |
| DeepSeek | 成功     | 成功     | —                      | `deepseek-v4.1-flash` |

11 次请求全部成功，结构化 schema 和问题／材料引用检查均通过，Host 状态为 completed，完整修订稿和独立复核均存在。报告总用量 **78,204 Token**，11 次都返回 usage；因没有配置实际价格，费用显示“未核定”。模型身份一致仅指网关返回声明，不是上游认证。

在此之前还完成一轮 76,330 Token 的真实合成验收，用于发现并修正编辑未接收最后一轮回应的问题；最终交付以本页 78,204 Token 的修复后结果为准。另有沙箱网络拒绝的尝试，其调用按预留额度保守记录，不声称账单为零。

脱敏回执见 [live-smoke-result.json](live-smoke-result.json)。生成的合成示例报告见 [examples/live-smoke-review.md](examples/live-smoke-review.md)。

## 明确未宣称的验收

- 没有替用户安装或重启日常使用的 DSH Profile；真实宿主插件安装后仍需确认入口与其具体版本、反向代理设置相容。
- 没有把 24 轮模拟测试宣称为数小时真实供应商耐久压测。
- OpenAI、Claude 原生协议经过模拟适配测试；当前账号的真实模型仍未接通，默认禁用。
- 不提供已完成的外部事实核验、多用户权限或公网部署能力。
